# No Section-Separator Comments

Do not add banner/divider comments whose only purpose is to visually separate sections of a file (e.g. `// ---------- Section Name ----------`). If a file needs section headers to be readable, it should be split into separate modules instead.
