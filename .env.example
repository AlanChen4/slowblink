# Copy to `.env.local` and fill in what you need.
# .env.local is gitignored. If you use Doppler instead, you can ignore this
# file — `doppler run -- pnpm dev` injects everything. See .claude/rules/doppler.md.
#
# ⚠ CLIENT-SIDE ONLY. Every var here ships inside the Electron app, visible
# to anyone who unpacks the build. NEVER put server-only secrets here —
# they belong in `supabase/.env.example` (Edge Function env). The
# `electron.vite.config.ts` envPrefix is a narrow allowlist of exactly
# these names, and `scripts/check-bundle-secrets.js` fails the build if
# anything server-only (service role key, Stripe secret, etc.) sneaks in.

# --- Supabase (needed for cloud sync / cloud AI) ---
# From `pnpm db:start` output, or the hosted project's API settings.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# --- Edge function base URL (overrides SUPABASE_URL for dev pointing) ---
SLOWBLINK_API_BASE=

# --- BYO AI keys (only if you're not using cloud AI through Supabase) ---
OPENAI_API_KEY=

# --- Cloudflare AI Gateway (only if routing BYO AI through a gateway) ---
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_GATEWAY_ID=default
